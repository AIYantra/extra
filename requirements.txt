# Extra — Production Dependencies Manifest
# Audited: 100% Enterprise-Clean & Free of Unverified Solo Binary Blobs

# ── Tier 1: Microsoft & Anthropic Official ─────────────────────────────────────
mcp>=1.0.0                      # Official Anthropic Model Context Protocol SDK
playwright>=1.47.0              # Official Microsoft Browser Automation (Chromium/Edge/WebKit)

# ── Tier 1: Python Software Foundation & Foundational OSS ──────────────────────
pillow>=10.4.0                  # High-speed image encoding and ROI manipulation
beautifulsoup4>=4.12.3          # Semantic DOM tree parsing
imagehash>=4.3.1                # Perceptual hash for closed-loop stall detection
numpy>=1.26.0                   # High-performance pixel array buffers
psutil>=6.0.0                   # Process & window metadata discovery

# ── Windows Dependencies ────────────────────────────────────────────────────────
pywin32>=306; sys_platform == 'win32'                    # Official Windows COM & Win32 extensions
comtypes>=1.4.0; sys_platform == 'win32'                 # Pure Python COM client for Microsoft UI Automation Core
mss>=9.0.1; sys_platform == 'win32'                      # Fast multi-monitor capture via Microsoft gdi32.dll

# ── macOS Native PyObjC Bridges ────────────────────────────────────────────────
pyobjc-core>=10.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
pyobjc-framework-Quartz>=10.0; sys_platform == 'darwin'
pyobjc-framework-ApplicationServices>=10.0; sys_platform == 'darwin'
pyobjc-framework-ScreenCaptureKit>=10.0; sys_platform == 'darwin'
